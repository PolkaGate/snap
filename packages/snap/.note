# To run in dev environment with polkagate/extension-dapp build in an appropriate path(see setDappPath file):

yarn && yarn dev && yarn start


# To Publish on NPM:

npx snapper -p ./packages/snap

## adjust version in package.json (and snap.manifest.json)
yarn && yarn build
cd packages/snap/
npm publish --auth-type=web 